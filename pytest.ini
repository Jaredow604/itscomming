[pytest]
testpaths = tests
markers =
    structural: Tests that audit code structure without DB
    mismatch: Tests that document known dimensional mismatches
    type_check: Tests for data type compatibility
